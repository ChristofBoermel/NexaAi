{
  "name": "@nexaai/matching",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'no lint yet'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@nexaai/config": "workspace:*",
    "@nexaai/types": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
